<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>

        <template id="scenario_information" name="scenario_information">
            <html>
                <t t-call="wms_scanner.scanner_html_head"/>
                <body class="font-poppins duration-500 min-h-screen">
                    <t t-call="wms_scanner.scanner_menu_body_header"/>
                    <t t-call="customize_scanner.information_body_main"/>
                    <t t-call="wms_scanner.scanner_script_js"/>
                </body>
            </html>
        </template>

        <template id="information_body_main" name="information_body_main">
            <main class="relative">
                <section id="move" class="screen_title">
                    <div class="container mx-auto px-5 pb-5 relative">
                        <t t-call="customize_scanner.scanner_body_information"/>
                        <t t-call="customize_scanner.scanner_line_information"/>
                    </div>
                </section>
            </main>
        </template>

        <template id="scanner_body_information" name="scanner_body_information">
            <table>
                <tbody>
                    <tr>
                        <td>
                            <t t-call="wms_scanner.wms_scanner_image_information"/>
                        </td>
                        <td>
                            <h1>Information</h1>
                        </td>
                    </tr>
                </tbody>
            </table>
        </template>

        <template id="scanner_line_information" name="scanner_line_information">
            <t t-if="data.get('step')">
                <t t-set="step" t-value="data.get('step')"/>
                <form class="flex flex-col items-center gap-2 landscape:gap-1 landscape:w-[49%]"
                      t-att-action="'./scanner?scenario={}&amp;step={}'.format(step.scenario_id.id, step.id)"
                      autoComplete="off"
                      method="post">
                    <t t-call="customize_scanner.scanner_input_scan"/>
                    <button type="submit" class="btn-green landscape:col-span-2">Valid</button>
                </form>
            </t>
        </template>

    </data>
</odoo>
